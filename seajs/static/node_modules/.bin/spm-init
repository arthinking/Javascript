#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../spm-init/bin/spm-init" "$@"
  ret=$?
else 
  node  "$basedir/../spm-init/bin/spm-init" "$@"
  ret=$?
fi
exit $ret
